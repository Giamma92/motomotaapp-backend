{
    "version": 2,
    "builds": [{
        "src": "index.js", 
        "use": "@vercel/node",
        "env": {
            "PUPPETEER_CACHE_DIR": "/tmp/puppeteer-cache"
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}